CREATE TABLE components (id SERIAL PRIMARY KEY, category VARCHAR(50) NOT NULL, brand VARCHAR(100) NOT NULL, name VARCHAR(255) NOT NULL, price INTEGER NOT NULL DEFAULT 0, image_url TEXT, specs JSONB NOT NULL DEFAULT '{}', created_at TIMESTAMP DEFAULT NOW());